{
  "format": "umbraflow-annotation-v2-behavior-1",
  "case_id": "receipt.stale_frame",
  "description": "A receipt minted for an earlier frame cannot authorize an action on a later frame.",
  "frames": [
    {
      "id": "frame.receipt.stale_frame.before",
      "ticket": "ticket-17",
      "measurements": [
        {"probe": "surface.shop.title", "result": "present", "confidence": 0.99},
        {"probe": "target.leave_button", "result": "present", "confidence": 0.98}
      ]
    },
    {
      "id": "frame.receipt.stale_frame.after",
      "ticket": "ticket-17",
      "measurements": [
        {"probe": "surface.loadout.title", "result": "present", "confidence": 0.99},
        {"probe": "target.leave_button", "result": "present", "confidence": 0.98}
      ]
    }
  ],
  "candidates": [
    {
      "surface": "shop",
      "layer": "scene",
      "identity": [{"probe": "surface.shop.title", "requires": "present"}],
      "bindings": [{"target": "leave_button", "actions": ["click"]}]
    },
    {
      "surface": "loadout",
      "layer": "scene",
      "identity": [{"probe": "surface.loadout.title", "requires": "present"}],
      "bindings": [{"target": "leave_button", "actions": ["click"]}]
    }
  ],
  "request": {
    "kind": "reuse_receipt",
    "mint": {
      "frame": "frame.receipt.stale_frame.before",
      "action": {"target": "leave_button", "kind": "click"}
    },
    "use": {
      "frame": "frame.receipt.stale_frame.after",
      "receipt_frame": "frame.receipt.stale_frame.before",
      "action": {"target": "leave_button", "kind": "click"}
    }
  },
  "expected": {
    "mint": {"resolution": "resolved", "receipt": "minted"},
    "use": {
      "resolution": "resolved",
      "action": "rejected",
      "reason": "stale_receipt_frame"
    }
  }
}
