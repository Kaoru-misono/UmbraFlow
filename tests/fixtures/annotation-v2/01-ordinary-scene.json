{
  "format": "umbraflow-annotation-v2-behavior-1",
  "case_id": "scene.ordinary",
  "description": "A unique scene is resolved and its declared action is authorized.",
  "frames": [
    {
      "id": "frame.scene.ordinary.001",
      "measurements": [
        {"probe": "surface.camp.title", "result": "present", "confidence": 0.99},
        {"probe": "target.leave_button", "result": "present", "confidence": 0.98}
      ]
    }
  ],
  "candidates": [
    {
      "surface": "camp",
      "layer": "scene",
      "identity": [{"probe": "surface.camp.title", "requires": "present"}],
      "bindings": [{"target": "leave_button", "actions": ["click"]}]
    }
  ],
  "request": {
    "kind": "resolve_and_authorize",
    "frame": "frame.scene.ordinary.001",
    "action": {"target": "leave_button", "kind": "click"}
  },
  "expected": {
    "resolution": "resolved",
    "surface_stack": ["camp"],
    "receipt": "minted",
    "action": "authorized",
    "diagnostics": []
  }
}
