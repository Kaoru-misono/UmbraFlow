{
  "format": "umbraflow-annotation-v2-behavior-1",
  "case_id": "surface.overlay_above_scene",
  "description": "An overlay is resolved above its compatible scene and receives the top action.",
  "frames": [
    {
      "id": "frame.surface.overlay_above_scene.001",
      "measurements": [
        {"probe": "surface.battle.identity", "result": "present", "confidence": 0.96},
        {"probe": "surface.enemy_inspect.identity", "result": "present", "confidence": 0.99},
        {"probe": "target.close_overlay", "result": "present", "confidence": 0.98}
      ]
    }
  ],
  "candidates": [
    {
      "surface": "battle",
      "layer": "scene",
      "identity": [{"probe": "surface.battle.identity", "requires": "present"}],
      "bindings": []
    },
    {
      "surface": "enemy_inspect",
      "layer": "overlay",
      "covers": ["battle"],
      "identity": [{"probe": "surface.enemy_inspect.identity", "requires": "present"}],
      "bindings": [{"target": "close_overlay", "actions": ["click"]}]
    }
  ],
  "request": {
    "kind": "resolve_and_authorize",
    "frame": "frame.surface.overlay_above_scene.001",
    "action": {"target": "close_overlay", "kind": "click"}
  },
  "expected": {
    "resolution": "resolved",
    "surface_stack": ["battle", "enemy_inspect"],
    "receipt": "minted",
    "action": "authorized",
    "diagnostics": []
  }
}
