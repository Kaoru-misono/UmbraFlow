{
  "format": "umbraflow-annotation-v2-behavior-1",
  "case_id": "surface.interrupt_above_any_scene",
  "description": "The same interrupt resolves above two different base scenes.",
  "frames": [
    {
      "id": "frame.surface.interrupt_above_any_scene.battle",
      "measurements": [
        {"probe": "surface.battle.identity", "result": "present", "confidence": 0.95},
        {"probe": "surface.network_retry.identity", "result": "present", "confidence": 0.99},
        {"probe": "target.retry", "result": "present", "confidence": 0.98}
      ]
    },
    {
      "id": "frame.surface.interrupt_above_any_scene.camp",
      "measurements": [
        {"probe": "surface.camp.identity", "result": "present", "confidence": 0.95},
        {"probe": "surface.network_retry.identity", "result": "present", "confidence": 0.99},
        {"probe": "target.retry", "result": "present", "confidence": 0.98}
      ]
    }
  ],
  "candidates": [
    {
      "surface": "battle",
      "layer": "scene",
      "identity": [{"probe": "surface.battle.identity", "requires": "present"}],
      "bindings": []
    },
    {
      "surface": "camp",
      "layer": "scene",
      "identity": [{"probe": "surface.camp.identity", "requires": "present"}],
      "bindings": []
    },
    {
      "surface": "network_retry",
      "layer": "interrupt",
      "covers": ["*"],
      "identity": [{"probe": "surface.network_retry.identity", "requires": "present"}],
      "bindings": [{"target": "retry", "actions": ["click"]}]
    }
  ],
  "request": {
    "kind": "resolve_and_authorize_each",
    "frames": [
      "frame.surface.interrupt_above_any_scene.battle",
      "frame.surface.interrupt_above_any_scene.camp"
    ],
    "action": {"target": "retry", "kind": "click"}
  },
  "expected": {
    "per_frame": [
      {
        "frame": "frame.surface.interrupt_above_any_scene.battle",
        "resolution": "resolved",
        "surface_stack": ["battle", "network_retry"],
        "receipt": "minted",
        "action": "authorized"
      },
      {
        "frame": "frame.surface.interrupt_above_any_scene.camp",
        "resolution": "resolved",
        "surface_stack": ["camp", "network_retry"],
        "receipt": "minted",
        "action": "authorized"
      }
    ]
  }
}
