{
  "format": "umbraflow-annotation-v2-behavior-1",
  "case_id": "transition.declared_vs_observed",
  "description": "A declared transition is policy; an observed transition is evidence and does not rewrite policy.",
  "declared_transitions": [
    {
      "id": "camp_to_training_confirm",
      "from": {"context": "camp", "surfaces": ["camp"]},
      "trigger": {"target": "training_button", "kind": "click"},
      "to": {"context": "camp", "surfaces": ["training_confirm"]}
    }
  ],
  "observed_transitions": [
    {
      "id": "observation-001",
      "from": {"context": "camp", "surfaces": ["camp"]},
      "trigger": {"target": "training_button", "kind": "click"},
      "to": {"context": "camp", "surfaces": ["shop"]},
      "frames": ["frame.transition.before", "frame.transition.after"]
    }
  ],
  "request": {
    "kind": "compare_transition_history",
    "declared_id": "camp_to_training_confirm",
    "observed_id": "observation-001"
  },
  "expected": {
    "declared_policy": "present",
    "observed_result": "unexpected",
    "model_update": "none",
    "required_diagnostic": "observed_destination_differs_from_declared_destination",
    "must_not_happen": "rewrite_declared_transition_from_one_observation"
  }
}
