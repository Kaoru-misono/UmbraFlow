# Third-party Python the CI-labelled tests import. The four repository gates in
# scripts/ deliberately use only the standard library and need nothing here;
# this exists for tools/annotate, whose backend validates every checked-in
# contract with the official Draft 2020-12 validator rather than a hand-rolled
# one. test-annotate-backend carries the CI label, so any job that runs
# `ctest -L CI` needs these installed.
jsonschema>=4.23,<5
referencing>=0.35,<0.37
