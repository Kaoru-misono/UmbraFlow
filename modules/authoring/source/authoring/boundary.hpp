#pragma once

namespace uf::authoring
{
    // The native module boundary is intentionally source-empty. The offline
    // authority, including acceptance and question policy, remains the single
    // writer in tools/annotate.
}
