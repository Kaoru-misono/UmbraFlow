[module]
name = engine
type = static
version = 0.1.0

[build]
unity_build = false

# The engine loads no RuntimeModel, resolves no semantic identity, and authorizes
# against no declaration:
# what it still needs -- the frame identity, the project fingerprint, the
# decoded template and the search -- lives in domain and vision.
#
# ocr is public because the session's constructor names IOcrEngine: reading text
# off an observation is an engine verb, and the engine binds the port its caller
# supplies exactly as it binds the frame source and the action sink. vision is
# public for the same reason: matchTemplate names GrayTemplateImage, so a caller
# holding one has to see the type.
[dependencies]
public = core domain ocr trace vision
