# The offline Project Kit boundary owns declared source inputs, generated
# artifact closures, complete digest manifests and immutable releases. A
# generated artifact is a closure member; a hand-written input is not, and is
# pinned by digest as an input instead.

[module]
name = project
type = static
version = 0.1.0

[dependencies]
public = core domain operator
# task, because the kit writes the genesis RuntimeArtifact at scaffold
# time and those bytes are the runtime's constant, not the kit's. It
# names the two file names, the two format numbers and the genesis
# document itself, and nothing else: the kit still interprets no
# RuntimeModel semantics and still links no loader of its own.
private = image json schema task

# The one thing this module does that is not arithmetic over files: starting a
# process. `project upgrade` fetches a release through curl and then runs the
# binary it just installed, because the schema a declaration is judged against
# is compiled into an executable and the process that installed one cannot
# speak for it. Everything platform-specific about doing that is under
# platform/, on the same terms as every other boundary in this repository.
[sources.windows]
include = platform/process-run-windows.cpp

[sources.other]
include = platform/process-run-posix.cpp
