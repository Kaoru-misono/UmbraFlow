# The offline Project Kit build boundary. It owns source input declarations,
# generated five-function adapters and build receipts. Generation reads only
# declared source paths, and replaces its output subtree on every build.

[module]
name = project
type = static
version = 0.1.0

[dependencies]
public = core
private = domain json
