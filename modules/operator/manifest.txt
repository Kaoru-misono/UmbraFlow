[module]
name = operator
type = static
version = 0.1.0

[build]
unity_build = false

# json, because a CanonicalJson now carries the value its bytes denote as well
# as the bytes: the schema owner parsed the document to judge its canonical
# form, and the ProjectPlugin boundary is that value. Naming json::Value in
# project-plugin.hpp is what keeps the parse from happening a second time
# further down. json declares `public = core` only, so the edge closes no cycle.
#
# script is public because this module's own headers name its types now:
# project-tool-program.hpp takes a script::ToolRuntimeDispatch and a
# script::PureDataProgram::Resource, and project-tool-dispatch.hpp hands one run
# a script::ScopedRunRequest. It compiled while it was private only through the
# transitive `public = task` -> `public = script` edge, which made a real part of
# this module's public surface a fact about task's dependency list.
[dependencies]
public = core domain json task script
private = trace operator_sqlite3 schema
