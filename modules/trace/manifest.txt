[module]
name = trace
type = static
version = 0.1.0

[build]
unity_build = false

# The single evidence stream every layer writes into. It sits below engine and
# task so both can emit the same schema: it needs vision's search stop reason
# and domain's ids, spaces, frame identity and error kinds, and nothing those
# depend on depends on trace.
#
# annotation left this list with umbraflow-trace/v3, which stopped carrying a
# page or an element identity: both are Luau values now, and what a trace names
# instead is the content hash of the template a match searched for.
[dependencies]
public = core domain vision
