[module]
name = trace
type = static
version = 0.1.0

[build]
unity_build = false

# Trace owns only the generic screenshot-free audit stream. Consumers define
# event and payload schemas; Trace never interprets their business semantics.
[dependencies]
public = core domain
