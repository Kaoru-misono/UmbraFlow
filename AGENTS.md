# AGENTS.md

Read **`CLAUDE.md`** first. It contains the always-on red lines and routes each
task to its canonical workflow under **`.claude/skills/`**. Those instructions
apply to every agent working in this repository.

Codex note: your ephemeral plan scratch dir is `.Codex/plans/`; the permanent record
of any finalized plan still goes to `docs/plans/YYYY-MM-DD-<topic>.md`.

Before any Git mutation, read and follow the `manage-git-changes` skill
completely.
